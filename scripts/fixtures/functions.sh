#!/usr/bin/env bash
# Fixtures for content/scripting/functions.md: deliberately none.
#
# Every example on that page defines its functions and calls them in the same block, so there
# is nothing to create. This file exists because a page opts in to the replay by having one:
# without it the page would be listed as unverified, which would be untrue.
