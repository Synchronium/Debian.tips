# Examples this page cannot replay in a batch, with how each was verified instead.

# Returns the caller's own public IP, which differs for every reader by definition, and needs
# the public internet to get it. Verified by hand from a networked Debian trixie box.
See the public IP a request would come from
