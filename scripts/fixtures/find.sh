#!/usr/bin/env bash
# Fixtures for content/commands/find/examples.yaml. Must match its `fixtures:` block.
#
# The tree itself is `mk_projects` in _common.sh, shared with the pages that document the
# same sample data. Why its modes, owners and mtimes are all pinned is explained there.

. /tmp/fixtures-common.sh

mk_projects
