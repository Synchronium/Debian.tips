#!/usr/bin/env bash
# Setup script for lesson-two, whose one documented block carries `verify: skip`. The page
# opted into the replay and the replay has nothing automated left to run on it — the case
# test/verificationStats.test.ts uses to pin that this is not the same as having no setup script at all.
