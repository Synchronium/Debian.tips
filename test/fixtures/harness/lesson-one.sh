#!/usr/bin/env bash
# Setup script for the fixture tree's `lesson-one` scripting page — see greet.sh for why these
# exist. This one opts a *prose* page into the replay, so the fixture build exercises the
# separate prose counting as well.
echo "hello"
